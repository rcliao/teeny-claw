# 2026-02-17

## Session: Orchestration Layer Research

### Context
- Reviewed OpenClaw internals: context construction, system prompt assembly, sessions, sub-agents, compaction, queue, memory
- Reviewed NanoClaw (qwibitai/nanoclaw) and PicoClaw (sipeed/picoclaw) source code for orchestration patterns
- Eric noted research-helper matters only for multi-provider; plan-doc can wait since conversation IS the planning surface
- Decided to review orchestration patterns before building orchestration layer

### Key Findings

**NanoClaw** (~500 LOC core):
- Single Node.js process, SQLite for state, WhatsApp I/O
- Per-group message queue with global concurrency limit (GroupQueue)
- Container isolation: each group runs Claude Code in Apple Container/Docker
- IPC via filesystem (JSON files in ipc/{group}/input/)
- Streaming output via sentinel markers in stdout
- Session = folder. Each group has its own CLAUDE.md, filesystem, container
- Cursor-based message tracking with rollback on error
- Idle timeout → close container stdin → container exits
- No structured context passing between groups — they're fully isolated

**PicoClaw** (~2K LOC core, Go):
- Message bus pattern (InboundMessage → AgentLoop → OutboundMessage)
- ContextBuilder assembles system prompt: identity + bootstrap files + skills summary + memory context + tool summaries
- Session manager: JSON files, in-memory map with mutex, summary field for compaction
- Tool loop: iterate LLM calls up to maxIterations, handle tool calls inline
- Subagent support via spawn tool (no sub-sub-agents)
- Very similar to OpenClaw but ~10x simpler
- Bootstrap files: AGENTS.md, SOUL.md, USER.md, IDENTITY.md (same pattern)
- Skills: summary injected, full SKILL.md read on demand (same pattern)

### Orchestration Patterns Comparison

| Aspect | OpenClaw | NanoClaw | PicoClaw |
|--------|----------|----------|----------|
| Language | TypeScript | TypeScript | Go |
| Context | Rich system prompt + injected files + tool schemas | CLAUDE.md per container | System prompt + bootstrap files |
| Session | JSONL + in-memory | SQLite + container state | JSON files + in-memory map |
| Queue | Lane-based FIFO, per-session + global | Per-group queue + global concurrency | Message bus, single consumer |
| Parallelism | Sub-agents on dedicated lane | Container per group | Subagent manager |
| Isolation | Optional sandbox (Docker) | Mandatory containers | Workspace restriction |
| Memory | Markdown + vector search | CLAUDE.md per group | MEMORY.md + daily files |
| IPC | sessions_send RPC | Filesystem JSON | Message bus (in-process) |
| Compaction | LLM summarization + memory flush | None (Claude Code handles) | LLM summarization |

### What Matters for teeny-claw Orchestration

The core orchestration loop is surprisingly simple across all three:
1. **Receive message** (from channel/bus/queue)
2. **Build context** (system prompt + history + tools)
3. **Call LLM** with tool definitions
4. **Execute tools** inline, append results
5. **Loop** until LLM stops calling tools or max iterations
6. **Save** session state
7. **Send** response

The hard parts are:
- **Context construction** — what goes in, what gets truncated, how to prioritize
- **Queue/concurrency** — serializing per-session while allowing cross-session parallelism  
- **Memory persistence** — surviving compaction, cross-session recall
- **Tool result routing** — getting output back to the right place

For teeny-claw's CLI tools, the orchestration layer would be much simpler:
- No channels/messaging (it's CLI)
- No real-time streaming needed
- Tools are external binaries, not in-process functions
- Context = prompt template + relevant data from other tools

## Session: Unit Tests for teeny-orchestrator (8:30 AM)

### What happened
- Assessed project state: teeny-orchestrator Phase 1 had 8/8 acceptance tests but ZERO Go unit tests
- Wrote 23 unit tests across 3 packages:
  - `pkg/session` (9 tests): CRUD, save/reload, compaction, copy safety, sanitize
  - `pkg/toolreg` (8 tests): register, discover, execute, schema building, error cases
  - `pkg/context` (6 tests): message assembly, bootstrap loading, truncation, tool summary
- All 23 unit tests + 8 acceptance tests pass, `go vet` clean
- Created GitHub repo `rcliao/teeny-orchestrator` and pushed

### Current state
- teeny-orchestrator: Phase 1 solid — 23 unit tests + 8 acceptance = 31 total
- Still no tests for `pkg/loop` (needs mock provider) or `pkg/provider` (needs API key)
- Next: mock provider for loop tests, OR Phase 2 (daemon/cron/heartbeat)

## Session: Loop & Provider Unit Tests (9:30 AM)

### What happened
- Wrote 7 unit tests for `pkg/loop` using a mock provider:
  - Simple response, tool call → response, LLM error, max iterations, session persistence, empty response fallback, truncate
- Wrote 5 unit tests for `pkg/provider`:
  - Defaults, Name(), no-API-key error, tool call response parsing, error response parsing, message JSON roundtrip
- All 35 unit tests + 8 acceptance = 43 total, go vet clean
- Committed and pushed to GitHub

### Current state
- teeny-orchestrator Phase 1: fully tested — all 5 packages have unit tests now
- **Next**: Phase 2 (daemon/cron/heartbeat mode) OR end-to-end test with real API key

## Session: Phase 2 — Daemon & Scheduler (10:30 AM)

### What happened
- Built `pkg/scheduler` — interval-based job scheduling with `@every <duration>` syntax
- Added `daemon` command with signal handling (SIGINT/SIGTERM graceful shutdown)
- Added `job list` and `job run <name>` commands for job management
- Daemon config loaded from `~/.teeny-claw/daemon.json`
- Wrote 6 unit tests for scheduler package
- All 41 unit tests + 8 acceptance tests pass, go vet clean
- Committed and pushed

### Current state
- teeny-orchestrator Phase 2: daemon + scheduler done, chat REPL was already in Phase 1
- 41 unit tests + 8 acceptance = 49 total tests
- **Next**: Phase 3 (eval-grounded context, heartbeat self-review) OR add cron expression support to scheduler

## Session: Phase 3 — Eval-Grounded Self-Review (10:50 AM)

### What happened
- Built `pkg/eval` — client that queries token-eval for recent call data and agent-memory for stored learnings
  - QueryRecentCalls, QueryLearnings, StoreLearning, BuildReviewSummary, BuildLearningContext
- Added `heartbeat` command — runs one LLM-powered self-review cycle analyzing call patterns, storing learnings
- Updated `pkg/context/Builder` — SetLearnings() + auto-injection into system prompt with truncation
- Updated `buildOrchestrator` to auto-load learnings from agent-memory (best-effort, graceful degradation)
- 10 new tests (5 eval, 3 context, 2 updated)
- All 51 unit tests + 8 acceptance = 59 total, go vet clean
- Committed and pushed to GitHub

### Current state
- teeny-orchestrator Phase 3 done — the self-improvement loop is complete:
  heartbeat → query token-eval → LLM analyzes patterns → store learnings in agent-memory → learnings injected into future context
- **Next**: Phase 4 ideas — cron expression support, multi-provider, README/docs, or start plan-doc tool

## Session: Phase 4 — Cron Expressions + README (11:30 AM)

### What happened
- Added standard 5-field cron expression parser (`pkg/scheduler/cron.go`)
  - Supports: `*`, `N`, `N-M`, `N-M/step`, `*/step`, comma lists, `7=Sunday` alias
  - `CronExpr.Matches(time.Time)` for matching
- Updated scheduler to handle both `@every` intervals and cron expressions in `shouldRun`/`shouldRunInitial`
- 8 new cron tests: parse valid/invalid, matching (weekday, every-5-min, Sunday alias, comma), shouldRun/shouldRunInitial integration
- Wrote comprehensive README.md: install, quick start, config, commands, tool manifests, daemon/scheduling, architecture, self-improvement loop
- All 59 unit tests + 8 acceptance = 67 total, go vet clean
- Committed and pushed to GitHub

### Current state
- teeny-orchestrator Phase 4 partially done (cron + README)
- Remaining Phase 4 ideas: multi-provider support, more docs
- **Next**: multi-provider adapter OR polish/docs OR start plan-doc research

## Session: Multi-Provider Support (12:30 PM)

### What happened
- Added OpenAI-compatible provider (`pkg/provider/openai.go`) with `WithBaseURL` for custom endpoints
- Works with OpenAI, Groq, Together, Ollama, any OpenAI-compatible API
- Added provider factory (`pkg/provider/factory.go`) — `provider.New(name, apiKey, model)`
- Updated `buildOrchestrator` to use factory instead of hardcoded switch
- 9 new tests (7 OpenAI provider + 2 factory)
- All 68 unit tests + 8 acceptance = 76 total, go vet clean
- Updated README with provider table
- Committed and pushed

### Current state
- teeny-orchestrator: Phase 4 nearly complete (cron, README, multi-provider done)
- Remaining: base URL config support (currently only via Go API), or polish/docs
- **Next**: Add `base_url` to config JSON for custom endpoints, OR start plan-doc research, OR wrap up Phase 4

## Session: base_url Config Support (1:30 PM)

### What happened
- Added `base_url` field to AppConfig and provider.Config
- Created `NewFromConfig(Config)` factory function alongside existing `New()` for backward compat
- Updated `buildOrchestrator` to pass base_url from config to provider factory
- Updated README with base_url config example (Ollama endpoint)
- 1 new test (TestFactory_NewFromConfig — basic, with base URL, anthropic, unknown)
- All 69 unit tests + 8 acceptance = 77 total, go vet clean
- Committed and pushed

### Current state
- **teeny-orchestrator Phase 4: COMPLETE** ✅
  - Cron expressions, README, multi-provider, base_url config — all done
- 77 total tests, all passing
- **Next**: Wrap up Phase 4 in ROADMAP/MEMORY, then project is feature-complete for now. plan-doc is deferred.
